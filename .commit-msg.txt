Exclude sourcemaps from packaged ASAR

Verification of production build revealed that `node_modules/**/*.map`
files were being bundled into the ASAR. Added `!**/*.map` and
`!**/*.d.ts.map` to electron-builder `files` so packaged releases do
not ship third-party sourcemaps.

Verified:
  - `npm run build:prod` succeeds
  - `out/` contains no `.map` files
  - `out/main/index.js` is minified (1-line, mangled identifiers)
  - `out/renderer/assets` contains no `console.log`/`warn`/`info`/`debug`
  - `npx electron-builder --win dir` packages successfully
  - `app.asar` contains no `.map` files after repackage

No code changes.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
