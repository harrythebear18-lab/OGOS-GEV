Visualize ocean currents and Argo drift trails on climate stations

- Add trajectory field to ClimateMeasurement type for Argo drift history
- Update erddap-fetcher Argo fetcher to collect last 30 positions per float
  (capped at 30 points per float to bound memory)
- Rewrite climate-stations plugin with full visualization:
  - Current vector arrows for TAO current stations (speed-scaled, color-coded)
  - Argo drift trail polylines (glowing lines showing recent positions)
  - Arrowhead points at current vector tips
  - Larger point size for Argo floats vs static buoys
  - disableDepthTestDistance for better visibility at distance
- Expose full measurement fields in entity properties:
  waterTemp, airTemp, windSpeed, windDir, waveHeight, wavePeriod,
  pressure, salinity, co2, currentSpeed, currentDir, depth, oxygen, ph,
  hasTrajectory, trajectoryPoints
- Add plugin controls: toggle current arrows, toggle drift trails,
  display counts for Argo floats and current sensors
- Add clear() method for per-layer clear button
- Update EntityInfoBox formatters for new fields:
  windDir (°), waveHeight (m), wavePeriod (s), salinity (PSU),
  co2 (ppm), currentSpeed (m/s), currentDir (°), depth (m/km),
  oxygen (µmol/kg), ph, trajectoryPoints (pts)
- Update README climate tier description

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
