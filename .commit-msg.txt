Wire color-transform WebGPU kernel — GPU color ramp mapping for band-math

Upgrade the color-transform WGSL kernel from a simple linear stretch
to proper color ramp interpolation on the GPU. The kernel now accepts
a packed ramp buffer [value0, r0, g0, b0, value1, r1, g1, b1, ...] and
outputs RGBA per pixel.

Update gpu-compute.ts execute() to handle:
  - 4x output size for color-transform (RGBA vs single float)
  - Extra ramp storage buffer binding
  - Ramp stop count in uniform data

Add ramp field to ComputePayload and pass through dispatcher.

Wire band-math plugin to use GPU color-transform:
  NDVI/NDWI/NBR result → color-transform kernel → RGBA canvas
  Falls back to JS color mapping if GPU unavailable

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
