Wire compute dispatcher — WebGPU slope/anomaly now touch real GPU

Define a single compute contract (src/shared/compute-contract.ts):
  task in → buffer out → backend tagged

Renderer compute dispatcher routes to best backend:
  1. WebGPU kernel (if available + grid ≥ 128x128)
  2. CPU worker pool via IPC (fallback)
  3. noop (caller falls back to legacy IPC)

Main-process compute fallback handles compute:task IPC → worker pool.

Wire slope plugin to dispatcher (WebGPU dem-slope → worker → legacy IPC).
Wire anomaly plugin to dispatcher (WebGPU anomaly → worker → legacy IPC).
Add terrain:dem:raw IPC for raw DEM grid data to renderer.
Remove dead gpu-manager.ts stub.
Update HAL.md + README truth tables.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
