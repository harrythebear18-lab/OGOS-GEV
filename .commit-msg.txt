Port OGOS integrity pipeline, incident workflow, explainability overlay, and network/grid UI

- Port four climate integrity services from OGOS: DataFlowMonitor,
  SensorVerifier, ResultsVerifier, HeuristicWatchdog. Wire them into
  ClimateMonitor so IntegrityUpdate now carries real sensor health,
  pipeline scores, and cross-verification arrays instead of empties.
- Extend IntegritySummary with sensor/pipeline/results layer scores
  and flag tallies; update ClimateIntegrityPanel to render them.
- Add IncidentPanel for the Fall -> Flow -> Find missing-person
  workflow (route plan, fall-risk map, remains corridor) with
  bounds validation and fly-to controls.
- Add ExplainabilityOverlay rendering AI hypothesis zones as
  confidence-colored polygons on the Cesium globe with a click-to-
  inspect reasoning popup. Extend AiPanel hypothesis schema to
  carry zone polygons.
- Add NetworkGridPanel exposing network (speed test, DNS test, VPN,
  GeoIP, alerts) and grid (assets, cross-domain toggle, snooze,
  alerts) operational controls.
- Extend preload + globals.d.ts with grid and network API surfaces;
  add IPC handlers for grid snooze/settings and climate snooze.
- Wire export collectResults() to gather real plugin results so
  GeoJSON/KML exports are no longer empty.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
