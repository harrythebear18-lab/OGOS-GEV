Fix hydrology/rainfall reliability + update README for single-window 3D cockpit

Hydrology fixes:
- Rainfall timeout increased from 5s to 15s (Open-Meteo was timing out)
- Added rainfall method to preload weather bridge + globals.d.ts type
- Added manual rainfall slider + override toggle to hydrology plugin
- Fixed no-retry-on-error (lastBbox now cleared on failure)
- Removed water fetch duplication from hydrology (dedicated water plugin handles it)
- Added per-entity count displays (flow paths, pools, flood zones, watersheds)
- Added isRunning guard to prevent concurrent analysis runs

Water/roads Overpass reliability:
- Added 2 more Overpass mirror servers (mail.ru, kumi.by)
- Added non-JSON response detection (some mirrors return HTML errors)
- Increased timeout from 15s to 25s
- Added error field to WaterResponse so plugin can surface failures
- Water plugin now shows error message in controls when all servers fail

README rewrite:
- Reflects single-window 3D Cesium cockpit (not multi-window, not 2D)
- Documents all 31 plugins grouped by tier
- Updated project structure (no map/2D, no gpu window, no ai window)
- Updated tech stack (removed MapLibre, added xmldom/adm-zip)
- v0.5 status with OGOS migration complete

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
