Fix two crashes: NetworkGridPanel rendering object arrays + preload missing unsub returns

- NetworkGridPanel: grid update payload contains arrays of asset/measurement
  objects, not counts. Use .length for display. Also fix NetHealth/NetVpn
  interfaces to match actual NetworkHealth/VpnStatus shapes (status,
  connectivityScore, latency, packetLoss, isActive, publicIP, vpnProvider).
- Preload: all on* subscription methods now return proper unsubscribe
  functions via a shared subscribe() helper. Previously they returned
  undefined, causing "unsub is not a function" crashes in components
  that expected a cleanup return (e.g. PredictionPanel).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
