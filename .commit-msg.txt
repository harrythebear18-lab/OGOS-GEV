Add elegant weather forecast + prediction engine UI overlays

- WeatherOverlay: floating panel with current conditions card (temp, feels-like,
  wind, humidity, precip, weather icon) + 24h hourly forecast chart (SVG temp
  line + precip probability bars) + radar/satellite toggle buttons
- PredictionPanel: floating panel subscribing to PREDICTION_UPDATE IPC channel,
  showing 7-model prediction engine output:
  - Summary stats (total, high risk, critical, avg confidence)
  - Severe weather alerts (expandable, severity/confidence badges)
  - Storm track predictions (expandable, with track points + wind speed)
  - Climate anomaly predictions (expandable)
  - Sensor failure predictions (expandable)
  - Precipitation forecast (expandable)
  - SST anomalies (region + deviation)
  - Teleconnection indices (ENSO, NAO, etc.)
- Add weather-codes.ts shared helper for WMO weather code descriptions
- Add predictions namespace to globals.d.ts type definitions
- Wire both overlays into App.tsx with toolbar buttons (INTEGRITY, WEATHER, PREDICT)
- Only one overlay visible at a time (mutually exclusive toggles)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
