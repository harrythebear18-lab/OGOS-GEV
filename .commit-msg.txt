Production hardening + compute benchmark harness (checklist items 18/19)

Build hardening:
  - electron.vite.config.ts: explicit `sourcemap: isProd ? false : 'hidden'`
    for main, preload, and renderer
  - Terser minification with `mangle.toplevel: true`, `toplevel: true`,
    `compress.passes: 3`, `drop_console` for log/info/warn/debug
  - `build:prod` / `dist:*` scripts run `NODE_ENV=production`
  - Production gating: FPS tracking only in `isDevRenderer`
  - `src/main/utils/is-prod.ts` and `src/renderer/globe/hal/is-prod.ts`
    for shared env gating

HAL compute benchmark harness:
  - New `src/renderer/globe/hal/compute-bench.ts`
    - inline JS reference kernels (ndvi, ndwi, nbr, slope, hillshade, anomaly)
    - `makeBenchPayload()`
    - `benchmarkWorkload()`
    - `downloadReport()` for JSON export
  - `compute-dispatcher.ts`:
    - Uses `selectBackendPriority()` with thresholds:
        < 128×128   → cpu-inline
        < 512×512   → wasm-simd → cpu-worker → cpu-inline
        >= 512×512  → webgpu → wasm-simd → cpu-worker → cpu-inline
    - `dispatchBackend()` now runs real inline JS via `computeInline()`
    - `benchmark()` tests all four backends including inline

Benchmark plugin:
  - Uses `compute-bench.makeBenchPayload()`
  - Added "Download JSON" button that exports `BenchReport[]`
  - Maps `cpu-inline`/`cpu-worker` to `inline`/`worker` in reports

Modified:
  electron.vite.config.ts
  src/renderer/globe/Globe.tsx
  src/renderer/globe/hal/compute-dispatcher.ts
  src/renderer/globe/plugins/benchmark-plugin.ts
  src/renderer/globe/hal/compute-bench.ts (new)
  README.md
  HAL.md

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
